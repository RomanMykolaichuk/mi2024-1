const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export function mount(element,config){const views=config.views??[];if(!views.length)return;element.innerHTML=`<div class="prep-tabs">${views.map((v,i)=>`<button class="prep-tab ${i===0?'is-active':''}" data-view="${esc(v.id)}">${esc(v.label)}</button>`).join('')}</div><div class="prep-panel" data-role="panel"></div>`;const panel=element.querySelector('[data-role="panel"]'),buttons=[...element.querySelectorAll('[data-view]')];function render(id){const v=views.find(x=>x.id===id)||views[0];buttons.forEach(b=>b.classList.toggle('is-active',b.dataset.view===v.id));panel.innerHTML=`<div class="prep-panel__head"><div><p class="eyebrow">${esc(v.kicker??'EDA')}</p><h3>${esc(v.title)}</h3></div><span class="prep-value">${esc(v.metric??'')}</span></div><p class="component-intro">${esc(v.description)}</p><div class="evidence-grid">${(v.evidence??[]).map(x=>`<article><strong>${esc(x.value)}</strong><span>${esc(x.label)}</span></article>`).join('')}</div><div class="analytics-callout"><strong>Аналітичний висновок:</strong> ${esc(v.insight)}</div>${v.risk?`<div class="risk-note"><strong>Ризик:</strong> ${esc(v.risk)}</div>`:''}`;}buttons.forEach(b=>b.addEventListener('click',()=>render(b.dataset.view)));render(views[0].id);}
